---
kind: milestone
title: 'M4: Live canvas'
target: edit a file, watch the browser follow within a second
---
FastAPI server on localhost, watchfiles pushing re-parsed plans over a WebSocket, React Flow rendering read-only with elk auto-layout and view.yaml positions. The packaging spike (built assets inside the wheel) lands first because it is the scary unknown.