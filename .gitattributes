# Normalize to LF in the repo; round-trip fidelity tests cover CRLF user files
# at runtime, but the repo itself stays LF so goldens are byte-stable on every OS.
* text=auto eol=lf
*.png binary
*.ico binary
