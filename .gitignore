# Python
__pycache__/
*.py[cod]
.venv/
dist/
build/lib/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage

# Frontend build products
node_modules/
kumihimo/server/static/

# Docs build output
site/

# Kumihimo plans' own advisory event log (K31) — .kumihimo/ under any plan
# root in this repo (plans/roadmap, examples/*): scaffold() now writes this
# same rule into every new plan's own .gitignore too (kumihimo/core/store.py).
.kumihimo/

# OS / editor noise
.DS_Store
Thumbs.db
